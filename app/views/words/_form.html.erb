<%= form_for('/',word)do |f| %>
  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row form-group">
    <label for="word_name" class="col-sm-2 col-form-label">Nombre:</label>
    <div class="col-sm-6">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <div class="row form-group">
  <label for="word_description" class="col-sm-2 col-form-label">Descripción:</label>
    <div class="col-sm-6">
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  </div>

   <%= button_tag(type: 'submit', class: "btn btn-primary", style: "margin-bottom: 10px") do %>
    <i class="fas fa-plus"></i> Agregar
   <%end%>
<% end %>
